<% if item_in_folder?(document.value_for(:id)) %>
  <div>This item is in <%= link_to 'Marked List', folder_index_path %></div>
<% else %>
  <%= render :partial=>'folder_form', :locals=>{:document=>document} %>
<% end %>
