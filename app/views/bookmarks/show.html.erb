<%= t(:show_bookmark, :title => h(@bookmark.title)) %>

<% sidebar_items << capture do %>
  <%= link_to t(:back_to_bookmarks), bookmarks_path %>
<% end %>

<%= link_to t(:edit), edit_object_path %>

<dl>
	<dt><%= t(:bookmark_title) %></dt>
	<dd><%= h @bookmark.title %></dd>
	
	<dt><%= t(:bookmark_tags) %></dt>
	<dd><%= h @bookmark.tag_list %></dd>
	
	<dt><%= t(:bookmark_notes) %></dt>
	<dd><%= h @bookmark.notes %></dd>
</dl>
